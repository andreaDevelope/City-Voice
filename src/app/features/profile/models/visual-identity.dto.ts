import { ProfileSymbol } from '../enums/profile-symbol';
import { ProfileColor } from '../enums/profile-color';

export interface VisualIdentityDto {
  symbol: ProfileSymbol;
  color: ProfileColor;
}
